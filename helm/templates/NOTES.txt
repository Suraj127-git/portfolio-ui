Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }} -n {{ .Release.Namespace }}
  $ helm get all {{ .Release.Name }} -n {{ .Release.Namespace }}

{{- if .Values.ingress.enabled }}

Your application should be available at:
{{- range .Values.ingress.hosts }}
  https://{{ .host }}
{{- end }}
{{- else }}

Get the application URL by running these commands:
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "portfolio-ui.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  echo "Visit http://127.0.0.1:8080 to use your application"
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 8080:80
{{- end }}

{{- if .Values.deployment.canary.enabled }}

CANARY DEPLOYMENT IS ACTIVE!
- Stable replicas: {{ .Values.deployment.stable.replicaCount }}
- Canary replicas: {{ .Values.deployment.canary.replicaCount }}
- Traffic is split between stable and canary versions

Monitor your canary deployment:
  kubectl get pods -n {{ .Release.Namespace }} -l version=canary
  kubectl logs -n {{ .Release.Namespace }} -l version=canary

To promote canary to stable, update your values with:
  deployment.canary.enabled=false
  deployment.image.tag=<canary-tag>
{{- end }}
